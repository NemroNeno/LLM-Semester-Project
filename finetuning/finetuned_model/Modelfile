# 1. Point to the base model you used for training
FROM qwen2.5:3b-instruct

# 2. Point to the current directory for the adapter weights
# Ollama will automatically find the .safetensors and .json files
ADAPTER .

# 3. Use the template from your chat_template.jinja (translated to Go)
TEMPLATE """{{ if .System }}<|im_start|>system
{{ .System }}<|im_end|>
{{ end }}{{ if .Prompt }}<|im_start|>user
{{ .Prompt }}<|im_end|>
{{ end }}<|im_start|>assistant
{{ .Response }}<|im_end|>"""

# 4. Set parameters to match your training/inference needs
PARAMETER stop "<|im_start|>"
PARAMETER stop "<|im_end|>"
PARAMETER temperature 0.0